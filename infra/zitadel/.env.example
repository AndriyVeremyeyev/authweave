# Local synthetic identity lab only. Generate .env with make setup-auth.
# Never reuse these credentials outside this isolated Docker Compose project.
AUTHWEAVE_ZITADEL_MASTERKEY=generate-with-make-setup-auth
AUTHWEAVE_ZITADEL_DB_PASSWORD=generate-with-make-setup-auth
AUTHWEAVE_ZITADEL_ADMIN_PASSWORD=generate-with-make-setup-auth
AUTHWEAVE_ZITADEL_LOGIN_PAT_EXPIRES=generate-with-make-setup-auth
